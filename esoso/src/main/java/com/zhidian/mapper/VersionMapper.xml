<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.zhidian.mapper.VersionMapper">

	<select id="queryVersionsForSearchService01SimpleVersion"
		resultType="Version" parameterType="hashmap">
		select
		id,name,version,type,defCss,defPage,defJs from versions where
		versions.type = #{type}
		and versions.name = #{name} and versions.using
		= 1;
	</select>

	<select id="queryVersionsForPageService01SimpleVersion"
		resultType="Version" parameterType="hashmap">
		select
		id,name,version,type,defCss,defPage,defJs from versions where
		versions.type = #{type}
		and versions.name = #{name} and versions.using
		= 1;
	</select>

	<select id="queryVersionsForPullArticleService01SimpleVersion"
		resultType="Version" parameterType="hashmap">

		select
		id,name,version,type,defCss,defPage,defJs from versions where
		versions.type = #{type}
		and versions.name = #{name} and versions.using = 1 
		<choose>
			<when test="version ==null or version==''">
				and version = '0.0.0.0'
			</when>
			<otherwise>
				and version = #{version}
			</otherwise>
		</choose>
	</select>

</mapper>
